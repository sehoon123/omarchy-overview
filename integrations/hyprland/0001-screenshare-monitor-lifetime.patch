diff --git a/src/managers/screenshare/ScreenshareManager.cpp b/src/managers/screenshare/ScreenshareManager.cpp
index 8d114b6..84c3ebf 100644
--- a/src/managers/screenshare/ScreenshareManager.cpp
+++ b/src/managers/screenshare/ScreenshareManager.cpp
@@ -51,6 +51,8 @@ UP<CScreenshareSession> CScreenshareManager::newSession(wl_client* client, PHLMO
     }
 
     UP<CScreenshareSession> session = UP<CScreenshareSession>(new CScreenshareSession(monitor, client));
+    if (!session->isActive())
+        return nullptr;
 
     session->m_self = session;
     m_sessions.emplace_back(session);
@@ -65,6 +67,8 @@ UP<CScreenshareSession> CScreenshareManager::newSession(wl_client* client, PHLMO
     }
 
     UP<CScreenshareSession> session = UP<CScreenshareSession>(new CScreenshareSession(monitor, captureRegion, client));
+    if (!session->isActive())
+        return nullptr;
 
     session->m_self = session;
     m_sessions.emplace_back(session);
@@ -78,7 +82,15 @@ UP<CScreenshareSession> CScreenshareManager::newSession(wl_client* client, PHLWI
         return nullptr;
     }
 
+    const auto PMONITOR = window->m_monitor.lock();
+    if (!PMONITOR || !State::monitorState()->contains(PMONITOR)) {
+        LOGM(Log::ERR, "Client requested sharing of a window without an available monitor");
+        return nullptr;
+    }
+
     UP<CScreenshareSession> session = UP<CScreenshareSession>(new CScreenshareSession(window, client));
+    if (!session->isActive())
+        return nullptr;
 
     session->m_self = session;
     m_sessions.emplace_back(session);
@@ -130,15 +142,15 @@ WP<CScreenshareSession> CScreenshareManager::getManagedSession(eScreenshareType
     if (it == m_managedSessions.end()) {
         UP<CScreenshareSession> session;
         switch (type) {
-            case SHARE_MONITOR: session = UP<CScreenshareSession>(new CScreenshareSession(monitor, client)); break;
-            case SHARE_WINDOW: session = UP<CScreenshareSession>(new CScreenshareSession(window, client)); break;
-            case SHARE_REGION: session = UP<CScreenshareSession>(new CScreenshareSession(monitor, captureBox, client)); break;
+            case SHARE_MONITOR: session = newSession(client, monitor); break;
+            case SHARE_WINDOW: session = newSession(client, window); break;
+            case SHARE_REGION: session = newSession(client, monitor, captureBox); break;
             case SHARE_NONE:
             default: return {};
         }
 
-        session->m_self = session;
-        m_sessions.emplace_back(session);
+        if (!session)
+            return {};
 
         it = m_managedSessions.emplace(m_managedSessions.end(), makeUnique<SManagedSession>(std::move(session)));
 
@@ -147,8 +159,10 @@ WP<CScreenshareSession> CScreenshareManager::getManagedSession(eScreenshareType
             if (!managed)
                 return;
 
-            const auto& session = managed->m_session;
-            std::erase_if(Screenshare::mgr()->m_managedSessions, [&session](const auto& s) { return s && s->m_session.get() == session.get(); });
+            // Erasure can destroy managed and its session before the predicate
+            // finishes scanning the remaining entries. Keep only the identity.
+            const auto session = managed->m_session.get();
+            std::erase_if(Screenshare::mgr()->m_managedSessions, [session](const auto& s) { return s && s->m_session.get() == session; });
         });
     }
 
diff --git a/src/managers/screenshare/ScreenshareManager.hpp b/src/managers/screenshare/ScreenshareManager.hpp
index d115d21..8b7bac8 100644
--- a/src/managers/screenshare/ScreenshareManager.hpp
+++ b/src/managers/screenshare/ScreenshareManager.hpp
@@ -90,7 +90,7 @@ namespace Screenshare {
         } m_listeners;
 
         void screenshareEvents(bool started);
-        void calculateConstraints();
+        bool calculateConstraints();
         void init();
 
         friend class CScreenshareFrame;
diff --git a/src/managers/screenshare/ScreenshareSession.cpp b/src/managers/screenshare/ScreenshareSession.cpp
index cae2d4d..893acec 100644
--- a/src/managers/screenshare/ScreenshareSession.cpp
+++ b/src/managers/screenshare/ScreenshareSession.cpp
@@ -9,30 +9,43 @@
 using namespace Screenshare;
 
 CScreenshareSession::CScreenshareSession(PHLMONITOR monitor, wl_client* client) : m_type(SHARE_MONITOR), m_monitor(monitor), m_client(client) {
-    if UNLIKELY (!m_monitor)
+    if UNLIKELY (!m_monitor) {
+        m_stopped = true;
         return;
+    }
 
     init();
 }
 
 CScreenshareSession::CScreenshareSession(PHLWINDOW window, wl_client* client) : m_type(SHARE_WINDOW), m_window(window), m_client(client) {
-    if UNLIKELY (!m_window)
+    if UNLIKELY (!m_window) {
+        m_stopped = true;
         return;
+    }
 
     m_listeners.windowDestroyed      = m_window->m_events.unmap.listen([this]() { stop(); });
     m_listeners.windowSizeChanged    = m_window->m_events.resize.listen([this]() {
-        calculateConstraints();
-        m_events.constraintsChanged.emit();
+        if (calculateConstraints())
+            m_events.constraintsChanged.emit();
     });
     m_listeners.windowMonitorChanged = m_window->m_events.monitorChanged.listen([this]() {
-        m_listeners.monitorDestroyed   = monitor()->m_events.disconnect.listen([this]() { stop(); });
-        m_listeners.monitorModeChanged = monitor()->m_events.modeChanged.listen([this]() {
-            calculateConstraints();
-            m_events.constraintsChanged.emit();
+        if (m_stopped)
+            return;
+
+        const auto PMONITOR = monitor();
+        if (!PMONITOR) {
+            stop();
+            return;
+        }
+
+        m_listeners.monitorDestroyed   = PMONITOR->m_events.disconnect.listen([this]() { stop(); });
+        m_listeners.monitorModeChanged = PMONITOR->m_events.modeChanged.listen([this]() {
+            if (calculateConstraints())
+                m_events.constraintsChanged.emit();
         });
 
-        calculateConstraints();
-        m_events.constraintsChanged.emit();
+        if (calculateConstraints())
+            m_events.constraintsChanged.emit();
     });
 
     init();
@@ -40,8 +53,10 @@ CScreenshareSession::CScreenshareSession(PHLWINDOW window, wl_client* client) :
 
 CScreenshareSession::CScreenshareSession(PHLMONITOR monitor, CBox captureRegion, wl_client* client) :
     m_type(SHARE_REGION), m_monitor(monitor), m_captureBox(captureRegion), m_client(client) {
-    if UNLIKELY (!m_monitor)
+    if UNLIKELY (!m_monitor) {
+        m_stopped = true;
         return;
+    }
 
     init();
 }
@@ -66,6 +81,14 @@ bool CScreenshareSession::isActive() {
 }
 
 void CScreenshareSession::init() {
+    // A mapped window can temporarily have no monitor during output removal.
+    // Reject it before reading the scale or attaching monitor listeners.
+    const auto PMONITOR = monitor();
+    if (!PMONITOR) {
+        stop();
+        return;
+    }
+
     uintptr_t ptr = m_type == SHARE_WINDOW && !m_window.expired() ? (uintptr_t)m_window.get() : (m_monitor.expired() ? (uintptr_t)nullptr : (uintptr_t)m_monitor.get());
     LOGM(Log::TRACE, "Created screenshare session for ({}): {}, {:x}", m_type, m_name, ptr);
 
@@ -82,22 +105,25 @@ void CScreenshareSession::init() {
 
     // scale capture box since it's in logical coords; round to integer pixel
     // dims so m_bufferSize matches the int32 size we send to the client
-    m_captureBox.scale(monitor()->m_scale).round();
+    m_captureBox.scale(PMONITOR->m_scale).round();
 
-    m_listeners.monitorDestroyed   = monitor()->m_events.disconnect.listen([this]() { stop(); });
-    m_listeners.monitorModeChanged = monitor()->m_events.modeChanged.listen([this]() {
-        calculateConstraints();
-        m_events.constraintsChanged.emit();
+    m_listeners.monitorDestroyed   = PMONITOR->m_events.disconnect.listen([this]() { stop(); });
+    m_listeners.monitorModeChanged = PMONITOR->m_events.modeChanged.listen([this]() {
+        if (calculateConstraints())
+            m_events.constraintsChanged.emit();
     });
 
     calculateConstraints();
 }
 
-void CScreenshareSession::calculateConstraints() {
+bool CScreenshareSession::calculateConstraints() {
+    if (m_stopped)
+        return false;
+
     const auto PMONITOR = monitor();
     if (!PMONITOR) {
         stop();
-        return;
+        return false;
     }
 
     // TODO: maybe support more that just monitor format in the future?
@@ -128,10 +154,11 @@ void CScreenshareSession::calculateConstraints() {
         default:
             LOGM(Log::ERR, "Invalid share type?? This shouldn't happen");
             stop();
-            return;
+            return false;
     }
 
     LOGM(Log::TRACE, "constraints changed for {}", m_name);
+    return true;
 }
 
 void CScreenshareSession::screenshareEvents(bool startSharing) {
diff --git a/src/protocols/Screencopy.cpp b/src/protocols/Screencopy.cpp
index 75c2302..0c2d2f1 100644
--- a/src/protocols/Screencopy.cpp
+++ b/src/protocols/Screencopy.cpp
@@ -64,6 +64,11 @@ CScreencopyFrame::CScreencopyFrame(SP<CZwlrScreencopyFrameV1> resource_, WP<CScr
     m_resource->setCopy([this](CZwlrScreencopyFrameV1* pFrame, wl_resource* res) { shareFrame(pFrame, res, false); });
     m_resource->setCopyWithDamage([this](CZwlrScreencopyFrameV1* pFrame, wl_resource* res) { shareFrame(pFrame, res, true); });
 
+    if (!m_session || !m_session->isActive()) {
+        m_resource->sendFailed();
+        return;
+    }
+
     m_frame = m_session->nextFrame(overlayCursor);
 
     auto formats = m_session->allowedFormats();
@@ -101,7 +106,7 @@ void CScreencopyFrame::shareFrame(CZwlrScreencopyFrameV1* pFrame, wl_resource* b
         return;
     }
 
-    if UNLIKELY (m_session.expired() || !m_session->monitor()) {
+    if UNLIKELY (!m_frame || m_session.expired() || !m_session->isActive() || !m_session->monitor()) {
         LOGM(Log::ERR, "Session stopped for frame {:x}", (uintptr_t)this);
         m_resource->sendFailed();
         return;
diff --git a/src/protocols/ToplevelExport.cpp b/src/protocols/ToplevelExport.cpp
index 4539ca5..8b7de6e 100644
--- a/src/protocols/ToplevelExport.cpp
+++ b/src/protocols/ToplevelExport.cpp
@@ -63,6 +63,11 @@ CToplevelExportFrame::CToplevelExportFrame(SP<CHyprlandToplevelExportFrameV1> re
     m_resource->setDestroy([this](CHyprlandToplevelExportFrameV1* pFrame) { PROTO::toplevelExport->destroyResource(this); });
     m_resource->setCopy([this](CHyprlandToplevelExportFrameV1* pFrame, wl_resource* res, int32_t ignoreDamage) { shareFrame(res, !!ignoreDamage); });
 
+    if (!m_session || !m_session->isActive()) {
+        m_resource->sendFailed();
+        return;
+    }
+
     m_frame = m_session->nextFrame(overlayCursor);
 
     auto formats = m_session->allowedFormats();
@@ -102,7 +107,7 @@ void CToplevelExportFrame::shareFrame(wl_resource* buffer, bool ignoreDamage) {
         return;
     }
 
-    if UNLIKELY (m_session.expired() || !m_session->monitor()) {
+    if UNLIKELY (!m_frame || m_session.expired() || !m_session->isActive() || !m_session->monitor()) {
         LOGM(Log::ERR, "Session stopped for frame {:x}", (uintptr_t)this);
         m_resource->sendFailed();
         return;
